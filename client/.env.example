# Backend API base URL. Defaults to http://localhost:4000 (lib/api.ts) if unset, so this
# is only needed to point at a non-default backend (e.g. the deployed Render URL).
VITE_API_URL=http://localhost:4000
